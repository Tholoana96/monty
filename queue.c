#include "monty.h"

/**
 * queue_ - changes to FIFO
 * @stack: monty stack
 * @_number: line number of file
 */

void queue_(stack_t **stack, unsigned int _number)
{
	UNUSED(stack);
	UNUSED(_number);
	STACKQ = 0;
}
